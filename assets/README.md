# Assets

This package adds:

- `directora-demo.gif` — lightweight animated governance-flow demo for the README.

The README also references:

- `directora-banner.png`

Keep your existing `assets/directora-banner.png` in the repository. It was not replaced by this package.
