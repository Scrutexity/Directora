# Assets

Expected assets:

- `directora-banner.png`
- `directora-demo.gif`

This package includes a lightweight placeholder `directora-demo.gif` so the README does not render with a broken demo image. Replace it later with a recorded/exported version of `components/ScrutexityFlow.tsx`.
